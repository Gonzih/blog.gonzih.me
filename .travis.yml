branches:
  only:
  - source

sudo: required

services:
  - docker

script:
  - rm -rf public || exit 0
  - make generate-using-docker

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: public
  target-branch: master
  keep-history: true
  on:
    branch: source
